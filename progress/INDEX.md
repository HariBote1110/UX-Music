# Progress Index

- [launchd-handoff.md](launchd-handoff.md) — Phase 0-3: `--install-agent`/`--uninstall-agent`でLaunchAgent plist（`~/Library/LaunchAgents/com.uxmusic.serve.plist`）を管理、launchctl呼び出しは`launchAgentRunner`インタフェース越しでテストは完全スタブ。GUI `Startup`で`/v1/identity`を照会し`roles`が`headless`なら`launchctl bootout`→失敗時`POST /v1/local/shutdown`フォールバック→ポート解放ポーリングでテイクオーバー、`roles`が`gui`（別GUI実行中）なら一切テイクオーバーしない方針。`Shutdown`で`bootedOutResidentAgent`が真なら`bootstrap`し常駐復帰、クラッシュ時は復帰しない旨を明記
- [serve-headless-mode.md](serve-headless-mode.md) — Phase 0-2: `--serve`（`--sync-serve`は非推奨エイリアス）を完全化。起動対象はLAN HTTPサーバ(mDNS込み)・Sync自動ループ・ライブラリ初期スキャンのみ（ローカル再生/MTP/CDリップ/OSメディアコントロール/デバイスウォッチャ/ダイアログは起動しない）。`/v1/identity`のJSON `roles`に`headless`/`gui`を追加（mDNS TXTのrolesは既存ピア互換のため不変）。ループバック限定`POST /v1/local/shutdown`をPhase 0-3 launchdハンドオフのフォールバックとして新設。Goサイドに周期スキャン/ウォッチャが存在しないため初期スキャン1回のみに留めた判断を記録
- [watch-transfer-resilience.md](watch-transfer-resilience.md) — 実機報告「アプリ閉じちゃうと転送止まっちゃう／画面OFFで文字盤に戻ると止まる」への対処3点。(1) Watch側で`WKExtension.shared().isFrontmostTimeoutExtended = true`を起動時に設定（frontmost猶予を2分→8分、ただしwatchOS 7以降非推奨で効果未保証）。(2) iPhone側`WatchTransferBridge.queue`の未送信サブセットをUserDefaultsへ永続化し、`activate()`完了時に`WatchTransferRestoreReconciliation`で`outstandingFileTransfers`/ダウンロード済み判定から再開計画を立てて実行（FIFO順保持、純粋関数はTDD）。(3) 転送中（preparing/sending）のみSettingsにfrontmost維持を促すフッターヒントを表示（`WatchTransferHintPolicy`）。恒久対策はWatch側バックグラウンドURLSession直接DL（`watch_transfer_research`、未着手）と結論
- [watch-transfer-ui-observation.md](watch-transfer-ui-observation.md) — 実機報告2件を調査・修正。(A)転送進捗がタブ切り替え時にしか更新されない件: `AppModel`（`@Observable`）に生の`ObservableObject`（`WatchTransferBridge`）をネストさせても`@Published`変更が伝播しない罠（`Observation`×`Combine`混在の罠、これで2件目）と特定。`WatchTransferQueueSection`/`WatchTransferSongMenuItemBody`/`WatchTransferBulkMenuItemBody`/`WatchTransferPlaylistMenuButton`という`@ObservedObject`購読専用の葉ビューを新設して解消。(B)「Watchへ転送の日本語が壊れてるかも」: カタログのja訳は正しいが`WatchTransferBulkMenuItem.title`が`String`型だったため`Label(_:systemImage:)`がverbatim表示オーバーロードに解決され常に英語表示になっていた真因を特定、`LocalizedStringKey`に変更して解消
- [watch-transfer-order.md](watch-transfer-order.md) — 実機報告「iPhone→Watch転送でアルバム内の曲順が入れ替わる」の真因と修正。オンデバイストランスコードが曲ごとの並行`Task`だったため`transferFile`呼び出し順（＝到着順とみなされていたトラック順）がトランスコード完了順にスクランブルされていた。`WatchTransferMeta`にtrack/discNumberを追加し`WatchAlbumGrouping`でソート、`WatchTransferBridge`をFIFOワーカー（`WatchTransferWorkQueue`）に置き換えて根本原因も解消、`WatchLibraryIndex.adding`を「先着優先」から「既存エントリを同位置で更新」に仕様変更し再送で旧データを修復可能に
- [transfer-progress-visibility.md](transfer-progress-visibility.md) — 転送/DLの進捗可視化。iPhone→Watch: `transferFile`はキュー投入のみで完了は`didFinish`まで届かないため`.sent`即時表示が「送信済みの嘘」になっていた真因を特定、`Phase.sending(Double)`をKVO(`fractionCompleted`)で観測しSettingsScreenに「完了 m/n」+集計ProgressViewを追加(`WatchTransferQueueSummary`)。PC→iPhone: `downloadAlbum`/`downloadPlaylistSongs`にアグリゲートバナー(`BulkDownloadStatusReducer`)を追加しLibrary/Remote両画面に共通配置、進捗書き込みを`ProgressPublishThrottle`（両タスクで共有）で約1%刻みに間引きperf finding 3を緩和（観測粒度自体は未解消）
- [localisation.md](localisation.md) — iOS/Watch アプリの多言語対応。`Localizable.xcstrings` を手書きで2つ導入（iOS/Watch各ターゲット、pbxprojへ手動登録）、キー=英語文字列でen/ja両方をtranslated登録。全View/Core/Service層の日本語ハードコードスイープが完了（220キー）。`pingResult`表示文字列の色分けをhasPrefix判定からBoolフラグに分離（翻訳するとロジックが壊れるため）。カタログ完全性テスト新設。追記（2026-08-11）: 残りファイル全対応完了、実機報告のホームタブバー言語混在バグ（`HomeRootView`の`Library`/`Remote`/`Control`キー未登録によるフォールバック）を特定・修正
- [upnext-swipe-and-ghosting.md](upnext-swipe-and-ghosting.md) — Up nextパネルの行スワイプ削除が横ページング（プレイヤーへ戻る）と競合していた問題をユーザー判断で解消：スワイプ削除を廃止しコンテキストメニュー「キューから削除」に統合、並べ替え（`.onMove`）は維持。実機報告「リストの描画に古いバージョンの面影が出てくる」の真因を特定：行IDが配列オフセット直結（`song.id#offset`）だったため削除/並べ替えのたびに後続行のIDがすべて変わりSwiftUIが作り直してクロスフェード、これが残像に見えていた。`PlaybackQueueEditing.occurrenceIdentities`（曲ID＋出現回数ベースの安定ID）に置き換えて解消
- [tab-accessory-scroll-reset.md](tab-accessory-scroll-reset.md) — 初回再生時にライブラリのスクロール位置がリセットされる不具合を修正。真因は`HomeRootView`の`uxMusicTabMiniPlayer`が`isEnabled`をif/elseで分岐し`.tabViewBottomAccessory`modifierの適用有無自体を切り替えていたため、最初の曲を再生した瞬間（nil→non-nil反転）に`TabView`サブツリー全体の構造的アイデンティティが変わり再構築されていたこと。iOS 26.1 SDKに専用オーバーロード`tabViewBottomAccessory(isEnabled:content:)`があることをswiftinterfaceから発見し単一呼び出しに変更、if/elseを撤廃。シミュレータのヘッドレス検証で空カプセルが出ないことを確認。スクロール位置維持自体は実機/データ入りライブラリでの確認が必要
- [download-audio-quality.md](download-audio-quality.md) — Desktop→iPhoneのダウンロードに音質選択（フル音質/AAC/フル+AAC）を追加。`DownloadRequestPlan`でダウンロード手順を導出、ステートレスな2ファイル併存（`<stem>.<ext>`＋`<stem>_aac.m4a`）でオリジナル解決ロジックから隠蔽、サーバのffmpeg未使用フォールバック（AAC要求でもオリジナルバイトが返る場合がある）を`AACVariantFinalisePlan`で判定table化。Watch転送は`watchTransferSourceURL`でAACバリアントを優先しオンデバイストランスコードをスキップ。設定は遡及適用されない。追記（2026-08-11）: AACビットレート選択（128/192/256/320、既定256）を追加。サーバは`bitrate`クエリ＋`<sha256>_<bitrate>.m4a`キャッシュ（128kbpsのみ旧形式キャッシュを再利用）、iOSは`DownloadAACBitrate`＋Settingsの menuピッカーで選択、Watch転送は無変更（256/320kbpsも既存ポリシーで自動128kbps化）
- [watch-transfer-transcode.md](watch-transfer-transcode.md) — Watch転送前にオンデバイスでAAC-LC 128kbps m4aへトランスコードする機能を追加。`watch-transfer-bottleneck.md`の調査結論（ライブラリ88%がFLAC、AAC128化で7〜8x削減）を受け、`WatchTransferAudioPolicy`（FLAC等は常にtranscode、mp3/m4a/aac/mp4は推定ビットレート>192kbpsのみtranscode）と`WatchAudioTranscoder`（AVAssetReader/Writer、Caches配下にキャッシュ、.tmp→リネームで原子的配置）を新設し`WatchTransferBridge.performTransfer`に統合。変換失敗時は原本へフォールバック、キューに`.preparing`（変換中）フェーズを追加
- [watch-ui-redesign.md](watch-ui-redesign.md) — （追記 2026-08-09 第4ラウンド）実機で「キュー&音量」ページに入るとスワイプ・Digital Crownが両方効かなくなる致命的フリーズを修正。真因はNow Playingページの不可視Crownフォーカスコントロールがオフスクリーンのまま`focus()`を呼び続け、SwiftUIのAttributeGraphが依存関係サイクルとしてメインスレッドを張り付かせていたこと（`sample`でCPU100%・全サンプルがfocus()経由のAttributeGraph更新経路と確認）。アートワーク同期デコード説とList/TabViewジェスチャー競合説はサンプリングで棄却。`7b8f532`（refocusTrigger導入）で混入したリグレッションと`bisect`で特定。Crownフォーカス要求をページの`onAppear`ではなく`isActive`（実際に選択中か）に紐付けて修正
- [watch-ui-redesign.md](watch-ui-redesign.md) — Watch UI を3ページ構成（ライブラリ⇄再生中⇄キュー&音量）に刷新: 自作トグルを `List` 掘り下げに、再生中はアートワーク全面背景化、音量は `WKInterfaceVolumeControl` ラップで新設、UI 文言を日本語統一。(a)Crown音量化・(b)メニュー経由の却下理由と `WatchSongRow` の `onSelect` 一般化を記録
- [mobile-library-ui.md](mobile-library-ui.md) — （追記 2026-08-08）検索行アクセサリのスワイプ時ポップを解消: 真因はタブ毎の `LibrarySearchRow` 別インスタンス生成で、全タブ共通の単一インスタンス化＋アクセサリのみクロスフェードに。`AlbumSortOrder`/`ArtistSortOrder` 新設で全タブにボタン常設、Remote も Local と同じ paged TabView へ統一
- [visualizer-perceptual-mapping.md](visualizer-perceptual-mapping.md) — ビジュアライザーのバー高さマッピングを聴覚特性ベースに刷新: 連続対数バンド(40/150/450/1400/4000/9000/16000Hz)のビン平均、ノイズフロア0.22でのゲート、dB領域向けのゆるいカーブ(pow 1.1)、等ラウドネス補正ゲイン、アタック0.55/リリース0.18の非対称スムージング（フレームレート非依存化）に置き換え、中央バー強調のsine乗数を撤去。純粋関数`computeBarHeights`を`visualizer-mapping.ts`へ切り出しTDDで実装。追記: 実音源で値が上限付近に張り付く問題を、帯域別AGC（直近ピーク正規化、即時アタック/半減期4秒減衰、ENV_FLOOR 0.25、コントラストpow 1.6）で解消し状態をオブジェクト化
- [visualizer-fft-normalisation.md](visualizer-fft-normalisation.md) — デスクトップ版ビジュアライザーのバー張り付きバグを修正: `calculateFFT` が未正規化FFTマグニチュードをdB変換していたため常に255に張り付いていた問題を、FFTサイズによる正規化とリニア領域での時間平滑化（AnalyserNode相当）で解消。合わせてGoデータ取得間隔を80ms→40msに短縮
- [mobile-library-ui.md](mobile-library-ui.md) — （追記 2026-08-07）ライブラリ行を角丸カードから全幅ベタ塗り（`LibraryListRowStyle`）＋固定行高へ、アルバム連結線を純粋関数 `AlbumGroupConnector` 化して行をまたいで連続表示、セグメント切替を paged `TabView` のスワイプに変更し競合する行スワイプを全廃（削除・キュー操作は長押しメニューへ集約）、For You 廃止、`.segmented` の等幅配分による文字切れを避けるため Picker を全幅の独立行に分離しアクセサリを検索行へ移動
- [mobile-desktop-parity-features.md](mobile-desktop-parity-features.md) — Mobile版 Desktop機能パリティ: 和訳表示（Desktopの`.ja.lrc`サイドカーをLAN APIで配信・`BilingualLyricsDisplayMode`を並行追加）、For You（`/v1/remote/situation-playlists`新設・生成ロジックはWails版と共有）、シャッフル/リピート（Watch側純粋ロジックの汎用化・リピートOFF不可の不具合も解消）、キュー編集、検索、ソート、アーティストビュー、Desktop準拠のアルバム単位アートワークまとめ表示とナビ再編の判断記録
- [mobile-nowplaying-visuals.md](mobile-nowplaying-visuals.md) — （追記）同期歌詞画面を Desktop 版（`applyLyricsMotion`）に忠実なコンテナオフセット方式へ刷新: 全行を実測高さ＋固定ギャップで積み上げアクティブ行をコンテナ高35%にアンカー、距離比例のステガー遅延(40ms)・ease 0.8秒、blurを廃止しアクティブ/非アクティブ二値の色・scale(1.091)に統一、フェード帯域をDesktopのmask-image(8%/70%)に合わせて変更。純関数 `nowPlayingLyricsLineOffsets`/`nowPlayingLyricsLineDelay` をTDDで追加し、`UXM_DEBUG_LYRICS_SONG` デバッグフック＋録画で実機相当の目視検証を実施
- [mobile-sync-failover.md](mobile-sync-failover.md) — （追記）接続先ホストの手動選択・固定モードを追加：`ServerConfig.preferredHost`/`activeHost`、純粋関数 `ConnectionCandidatePolicy.hostsToTry`、Settings の既知ホスト一覧UI（到達確認・Tailscale/LANバッジ・タップで固定化）
- [mobile-youtube-embed.md](mobile-youtube-embed.md) — iPhone版 YouTube 公式再生: フェーズ1〜Bでライブラリ統合・常駐WKWebView化を実施。フェーズCで「特定動画が埋め込み不可（error 150）」報告を調査し、UA偽装・mute同期・メッセージハンドラのフレーム越境露出隔離のいずれも効果がないことを確認、実Safariでも同一URLで同じエラーが再現することから**YouTube側のモバイルWeb埋め込み制限（クライアント側では回避不能）**と結論。フェーズDでフォールバックUXを実装: error 101/150時に「YouTube で開く」ボタン（`youtube://`優先、なければSafari）を表示し、キュー再生中は3秒後に自動で次の曲へスキップ（トースト表示）。UI統一フェーズで専用プレイヤー画面（`YouTubePlayerScreen.swift`）を廃止し、YouTube曲のタップは通常曲と同じくキュー再生→`NowPlayingView`（サムネイル読み込み中プレースホルダ＋フェードイン、サムネイル起点のアンビエントパレット抽出）に一本化
- [desktop-youtube-gate-removal.md](desktop-youtube-gate-removal.md) — デスクトップ版 YouTube 機能の `enableYouTube` 全体ゲートを撤廃し新規インストール直後から利用可、既定の再生モードを公式再生(embed)に変更、ダウンロード／ストリーミング(非公式)モードの選択肢のみを `enableYoutubeAdvancedModes`（旧ゲート機構を流用・リネーム）で引き続き隠す
- [mobile-qr-pairing-multi-nic.md](mobile-qr-pairing-multi-nic.md) — QRペアリングが複数NIC環境（Wi-Fi/有線/Tailscale等）で失敗する不具合の修正: `BuildPairingURL`のhosts=に全LAN IPv4を埋め込み、モバイル側は`RemoteConnectionResolver`で到達可能な候補を探してからredeem・fallbackHosts保存、パース失敗時もpairingErrorをサイレントにしない
- [watch-integration.md](watch-integration.md) — UX Music Mobile: Apple Watch 再生機能の移植（フェーズ2〜5）。共有 `WatchTransferMeta`/`WatchLibraryIndex`（iOS/watchOS 両ターゲット）、iOS 送信ブリッジ、watchOS 受信・永続化・AVPlayer 再生・UI。フェーズ5でLibrary⇄Now Playingの水平ページング化・Digital Crownシーク・MPNowPlayingInfoCenter/MPRemoteCommandCenter連携を追加。フェーズ8でWatch側バグ3件（一括転送での曲欠落調査／シークバーのtintフラッシュとレイアウト overflow 修正／Now Playingのアートワーク再デコードによるスタッター対策）に対応。フェーズ9でバグ1の真因を特定・修正: 受信側ではなく送信側 `WatchTransferBridge.send` が未ダウンロード曲をキューにも載せず黙ってスキップしていたのが原因で、`downloadHandler` 注入による自動ダウンロード→転送化と一括転送フィルタ撤廃で解消
- [mobile-device-identity-zero-uuid.md](mobile-device-identity-zero-uuid.md) — UX Music Mobile: `DeviceIdentity` がシミュレータ等で返るゼロUUIDを有効な識別子として誤採用し、デスクトップ側 `deviceAuthTokens` に衝突登録される不具合の修正（`resolvedDeviceId` への切り出しとフォールバック生成）
- [mobile-lan-api-v1-migration.md](mobile-lan-api-v1-migration.md) — UX-Music-Mobile を新 LAN API v1 に追従: wear 命名の全廃（RemoteAPIClient 等への改名）、Bearer 認証、secret→redeem 方式のペアリングへの変更
- [lan-api-v1.md](lan-api-v1.md) — LAN API v1 統一プロトコル仕様: `/wear/*`・`/sync/*` を `/v1/remote/*`・`/v1/sync/*` に再編、デバイス別トークン認証（`deviceAuthTokens`）・Bearer 一本化・QR→redeem 発行ペアリング・JSON エラー形式・バージョニング1体系化の決定記録
- [mobile-remote-control-footer.md](mobile-remote-control-footer.md) — UX Music Mobile: Remote Control タブのトランスポート行がミニプレイヤー＋タブバーのフッターに重なるバグの原因（`.ignoresSafeArea()` を持つ背景をZStackの兄弟にするとレイアウトサイズが背景に引きずられる）と修正
- [mobile-library-ui.md](mobile-library-ui.md) — UX Music Mobile: Local/Remote ライブラリ画面のタブ位置固定（共通 `LibrarySegmentedHeader`）と、行・グリッド・空状態の見た目統一（ellipsis メニュー集約、`ContentUnavailableView` は List 外に配置）
- [mobile-nowplaying-visuals.md](mobile-nowplaying-visuals.md) — UX Music Mobile: 再生画面スワイプ時のセーフエリア帯グラデーション侵食バグ修正（`nowPlayingSidePanelCoverage` による黒被覆レイヤー）と、歌詞画面（`NowPlayingLyricsScreen`）の Apple Music 風環境光デザイン刷新（背景共有・エッジフェード・タップシーク・自動スクロール一時停止）
